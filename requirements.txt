# Exact-pinned (unlike the range-pinned deps below) — bump deliberately and
# test before widening to a range.
fastmcp==3.3.1
PyJWT>=2.10.1,<3
httpx>=0.28.1,<1
starlette>=0.40.0,<1
uvicorn>=0.32.0,<1
